---
import Base from '../layouts/Base.astro';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { parse } from 'yaml';

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = join(__dirname, '..', '..');
const doc = parse(readFileSync(join(root, 'data', 'plans.yaml'), 'utf8'));
const meta = doc.meta;
---

<Base path="/method" title="评分方法 · Coding Plan Guide" description="怎么挑的？公式全公开。">
  <section class="reading" style="padding-top:3rem">
    <p class="eyebrow"><span class="bar"></span>方法</p>
    <h2>为什么是这几款？</h2>
    <p class="pull">不说废话。先过滤再打分，你关心什么它就比什么。</p>

    <h3 style="margin-top:2.5rem">一、先过两道筛</h3>
    <div class="formula">通过条件：价格 ≤ ¥{meta.price_cap} AND 模型排名国内前 4</div>
    <p>超预算（>¥{meta.price_cap}/月）的一律不入榜——不是它们不好，是大多数人不需要。旗舰模型不在国内前4的也不入——省那十几块用弱模型不值。</p>
    <p>国内排名参考 <a href="https://artificialanalysis.ai/leaderboards/models" target="_blank" rel="noopener">Artificial Analysis Intelligence Index</a> v4.1 榜单：国内第1=GLM-5.2(全球#7)，国内第2=Qwen3.7 Max(全球#11)，国内第3=MiniMax-M3(全球#12)，国内第4=DeepSeek V4 Pro(全球#13)。</p>

    <h3 style="margin-top:2.5rem">二、过了筛的怎么比</h3>
    <div class="formula">综合分 = 能力分×0.80 + 价格分×0.60 + 用量分×0.60 + 体验加分（满分100，加分上限20）</div>

    <p><b>能力分（0/5/10/25/50）</b>：按旗舰模型国内排名梯级赋分——国内第1=50，第2=25，第3=10，第4=5。GLM-5.2 占绝对优势，拉开明显差距。</p>

    <p><b>价格分（0-50 线性）</b>：50×(1 − 价格/¥150)。¥0→50，¥75→25，¥150→0。比梯级更精细，每一块钱都改变分数。</p>

    <p><b>用量分（0-50 线性）</b>：(月请求数 ÷ 200000)×50。月请求越高得分越高，满50分需要20万次请求。</p>

    <p><b>体验加分（0-20）</b>：反映实际使用感受的加分项——模型池大小、购买难度、首月优惠、模型厂商多样性、旗舰排名、地区友好度。好用的套餐不止是纸面数据好。</p>

    <h3 style="margin-top:2.5rem">三、权重怎么来的</h3>
    <p>能力 40% > 价格 30% = 用量 30%。模型能力是根本——再便宜再能扛，模型不行白搭。价格和用量同等重要。体验加分是上限20分的额外红利，不属于强制权重，但高分套餐一定有它的道理。</p>

    <h3 style="margin-top:2.5rem">四、排序规则</h3>
    <p>完全按公式得分从高到低排序。不加人工干预，分数就是排名。得分相同的按价格从低到高排。</p>

    <h3 style="margin-top:2.5rem">五、未过线的还看不看？</h3>
    <p>"全部"页面照样列出来，标注了被筛原因（超预算 / 模型不够强）。也许你预算没上限，自己去对比就好。</p>

    <h3 style="margin-top:2.5rem">六、中立声明</h3>
    <p>数据以各平台官方公布为准。每一条数据都手动核过，如果你发现错漏，欢迎提 issue 反馈。</p>

    <p style="margin-top:3rem;color:var(--ink-faint);font-size:.85rem">数据更新:{meta.updated} · 模型排名来源 <a href="https://artificialanalysis.ai/leaderboards/models" target="_blank" rel="noopener">Artificial Analysis</a></p>
  </section>
</Base>
